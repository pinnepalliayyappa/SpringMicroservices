server:
  port: 9100

spring:
  application:
    name: CallDetailsMS
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
      config:
        fail-fast: false
        enabled: true
        data-key: data
        defaultContext: application
        profileSeparator: '::'
        format: YAML
        prefixes: config/config













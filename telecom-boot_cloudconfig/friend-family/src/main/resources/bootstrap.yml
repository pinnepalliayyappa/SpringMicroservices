server:
  port: 9102

spring:
  application:
    name: FriendFamilyMS
  cloud:
    consul:
      host: localhost
      config:
        fail-fast: false
        enabled: true
        data-key: data
        defaultContext: application
        profileSeparator: '::'
        format: YAML
        prefixes: config/config






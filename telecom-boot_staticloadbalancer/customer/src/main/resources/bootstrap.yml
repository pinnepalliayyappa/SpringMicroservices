server:
  port: 9104

spring:
  application:
    name: CustomerMS
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
      config:
        fail-fast: false
        enabled: true
        data-key: data
        defaultContext: application
        profileSeparator: '::'
        format: YAML
        prefixes: config/config






